---
template: blog_post.html
title: 9.5 Terminal Linux Apps
description: أول 9 تطبيقات أُثبّتها على أي نظام لينكس (سيرفر أو جهاز مكتبي)
date: 2026-02-19
---

# <div dir="rtl">أول 9 تطبيقات أُثبّتها على أي نظام لينكس (سيرفر أو جهاز مكتبي)</div>

![type:video](https://www.youtube.com/embed/-IEY3wfSxYg)

<div dir="rtl">
أشارك معكم أهم 9.5 تطبيقات أستخدمها على أي توزيعة، سواء كان خادم أو جهاز مكتبي، أدوات حديثة للطرفية في لينكس ستجعل تجربتك أسرع، أوضح، وأكثر احترافية!
</div>

<p hidden>#more</p>

## update
- update system
```bash
sudo apt update
sudo apt upgrade
```

## bat – Modern `cat` with syntax highlighting
- Install
```bash
sudo apt install bat
```
- Setup
```bash
mkdir -p ~/.local/bin/
ln -s /usr/bin/batcat ~/.local/bin/bat
```

## duf – Disk usage / df replacement
- Install
```bash
sudo apt install duf
```

## eza – Modern `ls` replacement
- Install
```bash
sudo apt install eza
```
- Setup
```bash
grep -qxF "# eza alias" ~/.bashrc || {
  {
    echo ""
    echo "# eza alias"
    echo "alias ll='eza -lah --icons --group-directories-first'"
    echo "alias ls='eza --icons --group-directories-first'"
  } >> ~/.bashrc
}
```

## btop – Modern system monitor (`htop` replacement)
- Install
```bash
sudo apt install btop
```

## fastfetch – System info for terminal (`neofetch` replacement)
- Install
```bash
sudo apt install fastfetch
```

## dust – Human-friendly disk usage (`du` replacement)
- Install
```bash
sudo apt install du-dust
```

## zoxide – Smart directory jumping

- Install
```bash
sudo apt install zoxide
```
- Initialize zoxide in shell (adds `z`/`zi`)
```bash
grep -qxF 'eval "$(zoxide init bash)"' ~/.bashrc >/dev/null || cat >> ~/.bashrc <<'EOF'

# zoxide setup
eval "$(zoxide init bash)"

EOF
```

## fzf – Fuzzy finder
- Install
```bash
sudo apt install git
```

```bash
git clone --depth 1 https://github.com/junegunn/fzf.git ~/.fzf
```

```bash
~/.fzf/install
```

## starship – Modern, fast shell prompt
- Install
```bash
sudo apt install starship
```
- Initialize Starship prompt in shell
```bash
grep -qxF 'eval "$(starship init bash)"' ~/.bashrc >/dev/null || cat >> ~/.bashrc <<'EOF'

# starship setup
eval "$(starship init bash)"

EOF
```

## Bonus: fzf + zoxide → `zi`
- Combines **zoxide ranking** with **fzf interactive selection**
- **Usage**:
```bash
zi                 # interactive directory jump
z <partial-dir>    # jump instantly to most-used directory
```

## fonts
- Install JetBrainsMono Nerd Font
```bash
mkdir -p ~/.local/share/fonts/JetBrainsMono
```

```bash
curl -sLO https://github.com/ryanoasis/nerd-fonts/releases/latest/download/JetBrainsMono.tar.xz
```

```bash
tar -xvf JetBrainsMono.tar.xz -C ~/.local/share/fonts/JetBrainsMono
```

```bash
rm JetBrainsMono.tar.xz
```

```bash
fc-cache -fv ~/.local/share/fonts
```

```bash
fc-list | grep "JetBrains"
```